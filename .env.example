# No server-side secrets are required.
# The contact form posts directly to Formspree (endpoint set in src/lib/site.ts),
# and the Book a call button uses the Google Calendar embed. Nothing here to fill in.
